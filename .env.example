# ============================================
# FluviaFleet — Environment Variables
# Copy this file to .env and fill in your values
# ============================================

# Supabase (PostgreSQL + Auth)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# Google Gemini AI
GEMINI_API_KEY=your-gemini-api-key

# AISStream.io (Satellite vessel tracking)
AIS_API_KEY=your-aisstream-api-key

# Stripe (Payment gateway)
STRIPE_SECRET_KEY=sk_test_your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Firebase (entire service account JSON as single-line string)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# n8n Automation
N8N_API_KEY=your-n8n-api-key
N8N_WEBHOOK_URL=https://your-n8n-instance.com

# Server
PORT=3000
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=https://fluviafleet.com

# WhatsApp via CallMeBot (GRATIS, sin tarjeta)
# No se necesitan variables de entorno: cada contacto tiene su propio API key
# que se obtiene mandando "I allow callmebot to send me messages" a +34 644 64 55 30
# El API key se guarda por contacto en la tabla whatsapp_contacts.callmebot_api_key

# Backend URL (para daily-check interno)
BACKEND_URL=https://riverhub-elite-360.onrender.com
